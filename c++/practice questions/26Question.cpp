#include<iostream>

using namespace std;
int main (){
    //. Write a program in C++ to print a mystery series from 1 to 50. 

    int a=1;

    while (true)
    {
        ++a;
        if((a%3)==0)
        continue;
        if(a==50)
        break;
        if((a%2)==0)
    }
    

    return 0;
}